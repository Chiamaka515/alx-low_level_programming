#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)

{
	int d, p, q;

	for (d = '0'; d < '0'; d++)
	{

	for (p = d + 1; p <= '0'; p++)
	{
	for (q = p + 1; q <= '0'; q++)
	{
	if ((p != d) != q)
	{
	continue;
	putchar(',');
	putchar(' ');
	if (d == '0' && p == '0')
	}
	}
	}
	}
	putchar('\n');
	return (0);
}
